org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
# Kotlin code style
kotlin.code.style=official
# Recommended R class flag (avoids resource conflicts)
android.nonTransitiveRClass=true
# Suppress warning about compile SDK (35 is fully supported, this flag was for previews)
# android.suppressUnsupportedCompileSdk=35  ← REMOVED: not needed for SDK 35
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
